## 1. Initialize OpenSpec

- [x] 1.1 Create the OpenSpec repository structure with the official CLI.
- [x] 1.2 Add project-level OpenSpec configuration and repository ignore rules for runtime state.

## 2. Capture Baseline Capabilities

- [x] 2.1 Define baseline capability deltas for the OpenAI-compatible API surface.
- [x] 2.2 Define baseline capability deltas for credential/session management and model discovery.
- [x] 2.3 Define baseline capability deltas for DSML tool calling and file/vision handling.
- [x] 2.4 Define baseline capability deltas for operations and deployment behavior.

## 3. Finalize Bootstrap

- [x] 3.1 Archive the bootstrap change into main specs.
- [x] 3.2 Run OpenSpec validation on the resulting artifacts.
